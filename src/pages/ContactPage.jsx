import React from 'react';
import './ContactPage.scss';

export default function ContactPage() {
    return (
        <div className="contact-page empty-canvas">
            {/* 
              Página intencionalmente vacía (Canvas Overlay).
              El fondo 3D (ExperienceApps) es visible detrás.
            */}
        </div>
    );
}
