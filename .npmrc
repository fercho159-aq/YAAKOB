; framer-motion 11.0.8 declares a React ^18 peer, but the project runs React 19
; (Chakra UI 2.8.2 pulls framer-motion in). This is how node_modules was already
; resolved locally; without it Vercel's `npm install` fails with ERESOLVE.
legacy-peer-deps=true
